-- Classify support tickets and draft customer replies
-- Full pipeline: CLASSIFY → DRAFT → FLAG

FROM tickets.csv
CLASSIFY type INTO [policy, claim, inquiry, complaint]
  WITH insurance_context USE ticket_samples
DRAFT reply WITH customer_reply
FLAG WHEN type IS complaint
OUTPUT triage_replies.json
